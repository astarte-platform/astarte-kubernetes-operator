apiVersion: v1
kind: Service
metadata:
  labels:
    app: astarte-dashboard
    astarte-service: dashboard
    component: astarte
  name: astarte-dashboard
  namespace: {{ astarte_k8s_namespace }}
spec:
  clusterIP: None
  ports:
    - port: 80
  selector:
    app: astarte-dashboard

