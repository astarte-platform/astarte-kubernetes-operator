apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ resources_computed_prefix }}dashboard-config
  namespace: {{ astarte_k8s_namespace }}
data:
  config.json: >
    {{ lookup('vars', 'components.dashboard.config', default=astarte_dashboard_config) | to_json }}
